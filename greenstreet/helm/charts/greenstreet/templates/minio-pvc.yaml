apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: minio-pvc
  namespace: greenstreet

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: {{ .Values.minio.storage }}
