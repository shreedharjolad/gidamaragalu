apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "greenstreet.fullname" . }}-test-connection"
  labels:
    {{- include "greenstreet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "greenstreet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
